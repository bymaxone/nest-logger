// Public API of @bymax-one/nest-logger (server entry).
// Intentional placeholder — populated as services, destinations, decorators,
// and module barrels are added. Empty export satisfies `isolatedModules`.
export {}
